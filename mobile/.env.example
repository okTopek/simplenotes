# API base URL the app talks to.
# On a simulator, localhost works. On a physical device (Expo Go),
# use your computer's LAN IP, e.g. http://192.168.1.50:8000
EXPO_PUBLIC_API_URL=http://localhost:8000
